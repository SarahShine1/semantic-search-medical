# Semantic Search Medical

Medical semantic search application using embeddings and vector database.

## Features
- Embedding generation
- Medical dataset search
- Streamlit interface
- Docker support

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
